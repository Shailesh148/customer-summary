{
  "name": "cdk",
  "version": "0.1.0",
  "scripts": {
    "build": "cd lambda && mvn -q package -DskipTests",
    "cdk": "cdk",
    "test": "jest",
    "synth": "npm run build && npx cdk synth",
    "deploy": "npm run build && npx cdk deploy"
  },
  "devDependencies": {
    "aws-cdk": "2.1141.0",
    "jest": "^30"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.268.0",
    "constructs": "^10.5.0"
  }
}
